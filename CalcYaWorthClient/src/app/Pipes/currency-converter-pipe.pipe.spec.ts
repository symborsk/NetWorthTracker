import { CurrencyConverterPipePipe } from './currency-converter-pipe.pipe';

describe('CurrencyConverterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyConverterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
